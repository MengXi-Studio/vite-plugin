VITE_APP_TITLE="Vite Plugin Playground"
VITE_API_URL=https://api.example.com
VITE_DEBUG=false

# proxyManager 环境变量覆盖（envPrefix: PROXY_）
# 设置后会覆盖对应规则（context 为 /api）的 target
# PROXY_API_TARGET=http://localhost:3000
